var React = require('react');

var CharacterSheet = require('./CharacterSheet');

class App extends React.Component{
  render() {
    return(
      <CharacterSheet />
    )
  }
}

module.exports = App
